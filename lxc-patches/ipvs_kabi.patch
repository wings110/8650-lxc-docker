--- a/common/include/linux/skbuff.h
+++ b/common/include/linux/skbuff.h
@@ -991,10 +991,6 @@
 #ifdef CONFIG_IPV6_NDISC_NODETYPE
 	__u8			ndisc_nodetype:2;
 #endif
-
-#if IS_ENABLED(CONFIG_IP_VS)
-	__u8			ipvs_property:1;
-#endif
 #if IS_ENABLED(CONFIG_NETFILTER_XT_TARGET_TRACE) || IS_ENABLED(CONFIG_NF_TABLES)
 	__u8			nf_trace:1;
 #endif
@@ -1072,9 +1065,22 @@
 #ifdef CONFIG_KCOV
 	u64			kcov_handle;
 #endif
-
+union {
+	struct
+	{
 	ANDROID_KABI_RESERVE(1);
 	ANDROID_KABI_RESERVE(2);
+	};
+		struct
+	{
+#if IS_ENABLED(CONFIG_IP_VS)
+	__u8			ipvs_property:1;
+#endif
+#if IS_ENABLED(CONFIG_IP_SCTP)
+	__u8			csum_not_inet:1;
+#endif
+	};
+};
 
 	); /* end headers group */
 

--- a/common/include/net/net_namespace.h
+++ b/common/include/net/net_namespace.h
@@ -57,7 +57,18 @@
 
 #define NETDEV_HASHBITS    8
 #define NETDEV_HASHENTRIES (1 << NETDEV_HASHBITS)
-
+struct net_ext {
+	struct net		*net;
+#if IS_ENABLED(CONFIG_IP_VS)
+	struct netns_ipvs	*ipvs;
+#endif
+#if defined(CONFIG_IP_SCTP) || defined(CONFIG_IP_SCTP_MODULE)
+	struct netns_sctp	sctp;
+#endif
+#if defined(CONFIG_NF_TABLES) || defined(CONFIG_NF_TABLES_MODULE)
+	struct netns_nftables	nft;
+#endif
+};
 struct net {
 	/* First cache line can be often dirtied.
 	 * Do not place here read-mostly fields.
@@ -166,10 +171,6 @@
 #endif
 
 	u64			net_cookie; /* written once */
-
-#if IS_ENABLED(CONFIG_IP_VS)
-	struct netns_ipvs	*ipvs;
-#endif
 #if IS_ENABLED(CONFIG_MPLS)
 	struct netns_mpls	mpls;
 #endif
@@ -189,8 +190,31 @@
 #if IS_ENABLED(CONFIG_SMC)
 	struct netns_smc	smc;
 #endif
+	/* Keep legacy tail padding footprint; overlay nft state when enabled. */
+	union {
+		struct {
+			/* Force padding: keep 16-byte tail reservation for ABI stability. */
+			u64	__net_tail_pad1;
+			u64	__net_tail_pad2;
+		};
+		struct {
+			struct net_ext *ext;
+		};
+	};
 } __randomize_layout;
 
+#if IS_ENABLED(CONFIG_IP_VS)
+#define net_ipvs_ptr(_net)	((_net)->ext->ipvs)
+#endif
+
+#if defined(CONFIG_IP_SCTP) || defined(CONFIG_IP_SCTP_MODULE)
+#define net_sctp(_net)		((_net)->ext->sctp)
+#endif
+
+#if defined(CONFIG_NF_TABLES) || defined(CONFIG_NF_TABLES_MODULE)
+#define net_nft(_net)		((_net)->ext->nft)
+#endif
+
 #include <linux/seq_file_net.h>
 
 /* Init's network namespace */

--- a/common/include/net/ip_vs.h
+++ b/common/include/net/ip_vs.h
@@ -37,7 +37,7 @@
 /* Generic access of ipvs struct */
 static inline struct netns_ipvs *net_ipvs(struct net* net)
 {
-	return net->ipvs;
+	return net_ipvs_ptr(net);
 }
 
 /* Connections' size value needed by ip_vs_ctl.c */

--- a/common/net/netfilter/ipvs/ip_vs_core.c
+++ b/common/net/netfilter/ipvs/ip_vs_core.c
@@ -2300,7 +2300,7 @@
 	/* Counters used for creating unique names */
 	ipvs->gen = atomic_read(&ipvs_netns_cnt);
 	atomic_inc(&ipvs_netns_cnt);
-	net->ipvs = ipvs;
+	net_ipvs_ptr(net) = ipvs;
 	if (ip_vs_estimator_net_init(ipvs) < 0)
 		goto estimator_fail;
@@ -2336,7 +2336,7 @@
 control_fail:
 	ip_vs_estimator_net_cleanup(ipvs);
 estimator_fail:
-	net->ipvs = NULL;
+	net_ipvs_ptr(net) = NULL;
 	return -ENOMEM;
 }
 
@@ -2354,7 +2354,7 @@
 		ip_vs_control_net_cleanup(ipvs);
 		ip_vs_estimator_net_cleanup(ipvs);
 		IP_VS_DBG(2, "ipvs netns %d released\n", ipvs->gen);
-		net->ipvs = NULL;
+		net_ipvs_ptr(net) = NULL;
 	}
 }
